---
name: init
type: skill
about: Creates the ~/src/ directory if it doesn't exist.
---

# Init

Creates `~/src/` if it doesn't exist.

**Behavior:**
1. Check if `~/src/` exists
2. If missing, create it
3. Return confirmation
