---
name: commit-validate
type: skill
about: Validates a commit message against conventional commits format.
---

# Commit Validate

Validates a commit message against conventional commits format: `<type>(<scope>): <summary>`

**Arguments:**
- `message` — the commit message to validate

**Output:** `valid` or `invalid` with the reason if invalid.
